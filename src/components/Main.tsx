import { Stack } from '@chakra-ui/react'

export const Main = (props) => (
  <Stack
    spacing="2.5rem"
    width="100%"
    alignItems="center"
    maxWidth="60rem"
    pt="2rem"
    px="1rem"
    {...props}
  />
)
